import { PageAdmin } from '../theme';
import AuthForm from './AuthForm';

const Authorisation = () => {
  return (
    <PageAdmin>
      <AuthForm />
    </PageAdmin>
  );
};

export default Authorisation;
